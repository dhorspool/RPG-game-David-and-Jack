import csv
import random
filename ="enemies.csv"
lect = open(filename)
fitxer = csv.reader(lect)
inventory=[]
badans = True
understood=True
crstats = []

  
  
enemiestats = []

for row in fitxer:
  enemiename=row[0]
  enemieattack=row[1]
  enemiedefense=row[2]
  enemiehp=row[3]
  enemies = row
  enemiestats.append([row[0],int(row[1]),int(row[2]),int(row[3])])

ratstats=enemiestats[2]
slimestats=enemiestats[0]
batstats=enemiestats[1]
enemie=[slimestats,batstats,ratstats]
slime=enemie[0]
bat=enemie[1]
rat=enemie[2]

  
print ("Hello Adventurer, What is your Name?")
crname = str(input(""))
print ("%s, right? Not the name I would have chosen." %(crname,))
print ("")
print ("Let me make some things clear Right off the bat:")
print ("With the exception of Your Name, All words and commands amd etc will need to be clear: If you are inputting a number, make it just a number. If you are making an action, make it just one word, all lowercase. Is that clear ? :")
print ("")
print ("Options: yes, no")

while understood:
  ans = str(input(""))
  if ans == "Yes":
    print ("Repat, in lowercase")
  elif ans == "No":
    print ("Repeat, in lowercase")
  elif ans == "no":
    print ("With the exception of You Name, All words and commands amd etc will need to be clear: If you are inputting a number, make it just a number. If you are making an action, make it just one word, all lowercase. Is that clear ? ")
    print ("")
    print ("Options: yes, no")
    
  elif ans == "yes":
    print ("Great! Now we can understand each other")
    understood = False
  else:
    print ("Options:")
    print ("yes")
    print ("no")
    
print ("Anyway, Lets get started:")
print ("")
print ("You awake in the land of Russia, a magical place where dreams come true, and magic and animals roam the land. You are a :")
print ("")
warrior = "Warrior, with 2 ATK, 2 DEF, and 16 HP"
mage = "Mage, with 4 ATK, 0 DEF, and 12 HP"
rouge = "Rouge, with 3 ATK, 1 DEF, and 14 HP"

crclass = ("")
while not crclass == "warrior" or not crclass == "mage" or not crclass == "rouge":
  if crclass == "warrior" or crclass=="mage" or crclass == "rouge":
    print ("Great!")
    break
        
  else: 
     print ("")
  print ("Please choose a correct class name")
  print ("")
  print (warrior)
  print (mage)
  print (rouge)
  crclass = str(input("So, What are you?"))
  
  

'''atack, defence, base hp and level'''

if crclass == "warrior" :
  cratk = 2
  crdef = 2
  crhp = 16
  crlevel = 1
  crstats.append(crname)
  crstats.append(cratk)
  crstats.append(crdef)
  crstats.append(crhp)
  crstats.append(crlevel)
  
elif crclass == "rouge":
  cratk = 3
  crdef = 1
  crhp = 14
  crlevel = 1
  crstats.append(crname)
  crstats.append(cratk)
  crstats.append(crdef)
  crstats.append(crhp)
  crstats.append(crlevel)

  
elif crclass == "mage":
  cratk = 4
  crdef = 0
  crhp = 12
  crlevel = 1
  crstats.append(crname)
  crstats.append(cratk)
  crstats.append(crdef)
  crstats.append(crhp)
  crstats.append(crlevel)

  
print ("")
print ("Well then, lets begin:")
print ("You have three paths. Were do you wanna go?")
print ("-Forest")
print ("-Cave")
print ("-Eden")
print ("")
answer=input("So where:")

if answer == "forest":
  print("ok. so to pass, Master Elenoid (a wierd basard) offers you the way if you answer his riddle:")
  riddleans=input("the riddle is: what walks with four legs in the morning, two in the afternoon and three at night?:")
  
 badans = True
  
  while badans:
    if riddleans == "man":
      print("u smart. u pass")
      break
    else:
      print("u dont pass")
      print(" ")
      print(" ")
      riddleans=input("the riddle is: what walks with four legs in the morning, two in the afternoon and three at night?:")
      
elif answer == "cave":
    print("ok. so to pass, Master Ori (a wierd bar tender) offers you the way if you answer his riddle:")
    riddleans=input("the riddle is: what ios tall when its young and short when its old?:")
  
badans = True  
  
while badans:
  if riddleans == "pencil" or "candle":
    print("u smart. u pass")
    break
  else:
    print("u dont pass")
    print(" ")
    print(" ")
    riddleans=input("the riddle is: what ios tall when its young and short when its old?:")
  
elif answer == "eden":
  print("ok. so to pass, Master Lord (a wierd reader) offers you the way if you answer his riddle:")
    riddleans=input("the riddle is: There was a plane crash and every single person died, who survived?:")
  
 badans = True  
  
 while badans:
   if riddleans == "married couples":
     print("u smart. u pass")
   break
   else:
     print("u dont pass")
     print(" ")
     print(" ")
     riddleans=input("the riddle is: There was a plane crash and every single person died, who survived?:")

else:
  print("You wrote it wrong")
  
  
    
  
  
      
    

